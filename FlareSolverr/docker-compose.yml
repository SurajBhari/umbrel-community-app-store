version: "0.1"

services:

  app_proxy:
    environment:
      APP_HOST: flaresolverr_server_1
      APP_PORT: 8191
    
  server:
    image: flaresolverr/flaresolverr:latest
    user: "8191:8191"
    init: true